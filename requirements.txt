# RobotPy version is mirrored from Zeus/pyproject.toml.
robotpy[commands2]==2026.2.2

PySimpleGUI
PyQt6
pygame
pydualsense
pytest

# Robot hardware dependencies. These packages are not available on Windows,
# and pip will abort the entire install if it tries to resolve them there.
gpiozero; sys_platform == "linux"
smbus; sys_platform == "linux"
